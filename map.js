//make double for each element and show the doubled values in an array
const numbers = [3, 5, 2, 7, 9];
// const doubleIt = num => num * 2;
const makeDouble = numbers.map(num=>num*2);

console.log(makeDouble);